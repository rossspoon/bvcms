@using CmsWeb.Areas.People.Models.Person
@using UtilityExtensions
@model CmsWeb.Areas.People.Models.Person.PersonModel

@helper ListItem(AddressInfo a)
{
    var id = "tab-" + a.Name;
    <li @(a.Preferred ? "class=active" : "")>
        <a href="#@id" data-toggle="tab">
            @a.Display
        </a>
    </li>
}
<div id="contact" class="tabbable group">
    <div class="sidebar_title">
        <h2>Addresses</h2>
    </div>
    <ul class="nav nav-tabs">
        @ListItem(Model.FamilyAddr)
        @ListItem(Model.PersonalAddr)
    </ul>
    <div class="tab-content">
        @Html.Partial("DisplayTemplates/Address", Model.FamilyAddr)
        @Html.Partial("DisplayTemplates/Address", Model.PersonalAddr)
    </div>
</div>
