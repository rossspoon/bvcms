@using CmsWeb
@model CmsData.Content
@{
    Layout = "/Views/Shared/SiteLayout.cshtml";
}
@section scripts
{
@Html.Script("/Content/js/jquery.textarea.js")
<script type="text/javascript">
    $(function () {
        $(".bt").button();
        $("textarea").tabby();
        $("#delete").click(function (ev) {
            ev.preventDefault();
            if (confirm("Are you sure you want to delete this entry?"))
            	window.location = "/Display/ContentDelete/@Model.Id"; ;
        });
    });
</script>
}
@using (Html.BeginForm("ContentUpdate", "Display", FormMethod.Post))
{ 
    <div class="blogbody">
    <h2 class="title">@Model.Name</h2>
    <a href="/Display/Index">Menu of Content</a>
    <hr />
    @Html.Hidden("id")
	 @Html.Hidden("name")
	 @Html.Hidden("title")
    <div>
    <textarea name="body" cols="80" rows="40" wrap='off' style="width:99%">@Html.Raw(@Model.Body)</textarea>
    </div>
	 <br />
    <input type="submit" class="bt" value="Save" /> &nbsp;&nbsp; <input type="button" id="delete" class="bt" value="Delete" />
    </div>
}