@using CmsWeb;
@model CmsWeb.Models.TransactionsModel
<div class="modalPopup">
<table cellpadding="4">
<tr><td>Description</td><td>@Html.TextBox("description")</td>
    <td>Test Transactions</td><td>@Html.CheckBox("testtransactions")</td></tr>
<tr><td>Name</td><td colspan="3">@Html.TextBox("name")</td></tr>
<tr><td>Start Date</td><td>@Html.DatePicker("startdt")</td>
    <td>End Date</td><td>@Html.DatePicker("enddt")</td></tr>
<tr><td>&gt; Amount</td><td>@Html.TextBox("gtamount")</td>
    <td>&lt;= Amount</td><td>@Html.TextBox("ltamount")</td></tr>
</table>
<input name="Submit" type="submit" class="submitbutton" value="Filter" />
<input name="Submit" type="submit" class="submitbutton" value="Export" />
<a href="/Manage/Transactions">clear</a>
</div>
<table id="Transactions" class="grid" cellpadding="4">
    <thead>
    <tr>
        <th><a href="#" class="sortable">Id</a></th>
        <th><a href="#" class="sortable">Tran Id</a></th>
        <th><a href="#" class="sortable">Appr</a></th>
        <th><a href="#" class="sortable">Date</a></th>
        <th><a href="#" class="sortable">Description</a></th>
        <th><a href="#" class="sortable">Name</a></th>
        <th><a href="#" class="sortable">RegAmt</a></th>
        <th><a href="#" class="sortable">Donate</a></th>
        <th><a href="#" class="sortable">Total</a></th>
        <th><a href="#" class="sortable">Due</a></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var t in Model.Transactions())
    {
        Html.RenderPartial("Row", t);
    }
    </tbody>
</table>
@{Html.RenderPartial("Pager2", Model.Pager);}

