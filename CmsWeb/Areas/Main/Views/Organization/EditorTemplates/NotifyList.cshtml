@using CmsData
@model string
<a id="notifylist" href="/Organization/NotifyIds">
<div class="block border">
<b>choose users to notify (confirmation comes from first)</b>
@{  var q = from p in DbUtil.Db.PeopleFromPidString(Model)
            select new { p.FromEmail, p.PeopleId };
    foreach (var p in q)
    {
    <div>@p.FromEmail &mdash; (@p.PeopleId)</div>
    }
}
</div>
</a>