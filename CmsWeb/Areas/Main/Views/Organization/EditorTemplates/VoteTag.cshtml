@model CmsData.RegSettings.VoteTag
@using UtilityExtensions
@using CmsWeb
    @using (var ci = Html.BeginCollectionItem("VoteTags"))
    {
      <li>@Html.Raw(ci.hiddenindex)
      @{ var prefix = ci.SuitableId; }
          @Html.HiddenFor(m => m.Id, new { @class = "renumberMe" }) 
        <ul>
    <li><div class="sgfee">Confirm:</div> <a href="#" class="editor" title="click to edit" tb="@(prefix)_Display">
    <div id="@(prefix)_Display_ro" class="block border">
    @if (Model.Display.HasValue())
    {
        @Html.Raw(Model.Display)
    }
    else
    {
        <i>click to edit</i>
    }
    </div></a><a class="del" href="#"><img align="top" src="/images/delete.gif" title="remove this item" /></a></li>
    @Html.TextAreaFor(m => m.Display, new { @class = "html" })
          <li><div class="sgfee">Confirm:</div> @Html.CheckBoxFor(m => m.Confirm) </li>
          <li><div class="sgfee">SmallGroup:</div> @Html.TextBoxFor(m => m.SmallGroup) </li>
          <li><div class="sgfee">Message:</div> @Html.TextBoxFor(m => m.Message) </li>
        </ul>
      </li>
  }

@*    <span class="keyword2">Terms:</span> 
        <textarea class="html" id="Terms" name="Terms.Html">@Html.Raw(Model.Terms.Html)</textarea>*@