@model CmsWeb.Models.OrganizationPage.MemberModel
@Html.Partial("ExportToolBar") 
&nbsp;<div style="clear: both"></div>
<p>
Count: <strong>@Model.Count()</strong>
<a class="filtergroupslink bt" href="#" title="Filter by name/group">Filter@(Model.isFiltered ? "(filtered)" : "")</a>
@if(User.IsInRole("Edit"))
{ 
<a href="/SearchAdd/Index/@Model.OrganizationId?type=pending" class="memberdialog bt">add pending members</a>
<a href="/Organization/AddFromTag/@Model.OrganizationId?pending=true" id="addfromtaglink" title="Add Pendings From Tag" class="bt">add from tag</a>
<a href="/OrgMembersDialog/Index/@Model.OrganizationId?pendings=true" title="Update Pending Members" class="memberdialog bt">update pending members</a>
 } 
</p>
@Html.Partial("MemberGrid", Model) 
