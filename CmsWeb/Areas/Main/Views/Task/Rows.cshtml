@model CmsWeb.Models.TaskModel
@if(Model.Count == 0)
{ 
<tr id="nomatch"><td colspan="6">No matching records.</td></tr>
} 
@foreach (var t in Model.FetchTasks())
{
    @Html.Partial("Row", t)
}


