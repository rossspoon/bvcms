@model IEnumerable<CmsWeb.Models.TaskModel.IncompleteTask>
@if (Model.Count() > 0)
{
<table class="grid">
    <thead>
    <tr>
        <th>Task</th>
        <th>Date</th>
        <th>About</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var t in Model)
    { 
        <tr>
            <td><a href="@t.link">@t.Desc</a></td>
            <td>@t.AssignedDt</td>
            <td>@t.About</td>
        </tr>
    }
    </tbody>
</table>
}